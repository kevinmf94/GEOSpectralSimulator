# Compile instructions

To compile the module is necessary to has installed Unreal Engine version 4.22.2 and open the Unreal Engine project on the UE4Editor. In this point is necessary use the methods that allows Unreal Engine to compile and generate packages